<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <TextView
        android:id="@+id/tvDialogChannelInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#64748B"
        android:layout_marginBottom="10dp" />

    <EditText
        android:id="@+id/etDialogAmount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Amount"
        android:inputType="numberDecimal"
        android:fontFamily="monospace" />

    <EditText
        android:id="@+id/etDialogTrxId"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Transaction ID (optional)"
        android:layout_marginTop="8dp" />

    <TextView
        android:id="@+id/tvDialogDateButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="\uD83D\uDCC5  Today"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="#0F766E"
        android:paddingHorizontal="10dp"
        android:paddingVertical="6dp"
        android:background="@drawable/bg_dashed_button"
        android:layerType="software" />
</LinearLayout>
